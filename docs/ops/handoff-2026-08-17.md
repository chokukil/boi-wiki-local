# BoI Second Brain — Ops / Handoff (2026-08-17 → 2026-08-18)

이 문서 하나로 신규 세션이 상태를 복원할 수 있도록 쓴다.
작성: 2026-08-18 (WSL 세션) · 대상: `2026-08-17-second-brain-improvement-plan.md` Phase 0–6

---

## 1. Repo 지도

| 역할 | 경로 | 상태 (2026-08-18) |
|---|---|---|
| **Harness (canonical)** | `C:\AI\second-brain` | branch `codex/second-brain-markdown-first-quality` — Phase 0–4 커밋 완료 |
| **Profile (Local Private, employee 2055186)** | `C:\AI\second-brain-2055186` | branch `codex/broadcast-ai-paper-demo-2055186` — skill 4부 sync 완료 |
| **Backup** | `C:\AI\second-brain-backup-2026-08-17` | 242M, 무결성 검증 PASS (harness 2218 / profile 1477 / inbox 33 파일) |
| **Inbox 원문** | `C:\Users\choku\OneDrive\문서\BoI-Second-Brain-2055186-Inbox` | 65M / 33 파일 — `preserve_originals: true` |
| **기존 백업** | `C:\AI\second-brain-backup-2026-07-12` | **디스크에 없음** (2026-08-17 삭제됨). 최신 백업은 위 2026-08-17 |

Skill 4부 (전부 byte-identical, `5fd01834f038a502…`, 36,109B):
- `C:\AI\second-brain\.agents\skills\boi-second-brain\SKILL.md` (canonical)
- `C:\AI\second-brain\.claude\skills\boi-second-brain\SKILL.md`
- `C:\AI\second-brain-2055186\.agents\skills\boi-second-brain\SKILL.md`
- `C:\AI\second-brain-2055186\.claude\skills\boi-second-brain\SKILL.md`

동일 규칙: `scripts/*.py` 37건 + `scripts/*.ps1` 13건도 harness↔profile byte-identical (Phase 2 검증기 등가로 확인, drift 0).

---

## 2. 검증 runbook

### 2.1 Receipt 검증 (템플릿: `e2e-verification-2026-08-17.json`)

템플릿 위치: `C:\AI\second-brain-2055186\data\boi\private\2055186\reports\ai-research-second-brain\generated-answers\e2e-verification-2026-08-17.json`

새 receipt 묶음을 검증할 때 동일한 구조를 따른다:

1. `scope`: 검증 대상 `*.receipt.json` 상대 경로 glob 명시
2. `receipts_checked` / `evidence_items_checked`: 숫자
3. `checks` — 최소 6축:
   - `receipt_sha256_equals_page_evidence_sha256` (pass N/N)
   - `origin_ref_parity_receipt_vs_page`
   - `original_identity_binding_declared` — **3요소(evidence_id, evidence_sha256, expected_origin_ref) 전부 있어야 pass. 빈 `{}`는 fail (fail-closed)**
   - `original_source_pdf_bytes_match_declared_evidence_sha256` (원문은 Inbox 폴더)
   - `answer_md_hash_and_byte_count`
   - `claim_bindings_present`
4. `repair`: 수정한 경우 `action` / `files_modified` / `no_content_or_prose_changes` / `no_remote_mutations` 명시
5. `final_state`, `open_items`
6. `local_only: true` + `remote_mutations: 0` (해당 없으면)

### 2.2 Sync 검증

- Windows: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify_profile_sync.ps1`
- WSL 등가: skill 디렉터리 + `scripts/*.py|ps1` 재귀 파일의 SHA256을 경로 접미사로 비교 (drift 0 = in sync)

### 2.3 테스트 스위트

- Windows PowerShell 런타임이 있어야 전체 green: `python -m pytest tests/ -q`
- **WSL에서 14건 실패는 환경 문제** (테스트가 `powershell.exe`를 직접 spawn → `Exec format error`). 대상: `test_repository_source_and_mcp.py` 7, `test_second_brain.py` 2, `test_second_brain_knowledge_governance_contract.py` 1, `test_query_answer_quality_contract.py` 1 (+동일 계열). 회귀 아님 — Windows에서 재실행하면 확인.

---

## 3. 금지 / 주의

| 항목 | 규칙 |
|---|---|
| **Local Private 공유 경계** | `data/boi/private/`, Inbox 원문, profile repo는 BoI Wiki·MCP·Team·Public으로 절대 공개 금지. 명시적 user-approved preview가 있는 경우만 예외. backup 폴더도 네트워크/타인 디스크로 복사 금지 |
| **Remote mutation** | `remote_auto_upload: false`. receipt 검증·수리 작업은 `remote_mutations: 0` 유지. remote push는 Windows Git Credential Manager 세션에서만 (WSL credential blocked — 미완료) |
| **Python-free 경로** | 세션 시작 체크(폴더 확인 등)는 Python·외부 창·업로드 없이 수행. skill의 `agent_session_check` 흐름 준수 |
| **AI fallback 허위 표시** | LLM 연결 실패 시 "연결 실패"라고 명시. fallback을 AI 답변인 것처럼 보여주기 금지 |
| **검색 0건** | 모델 기억으로 채우기 금지. 조건을 정확히 서술하고 intake/review 제안 (skill Pitfalls 참고) |

---

## 4. 상태 스냅샷 (2026-08-18 종료 시점)

### 완료
- **Phase 0 (위생)**: stale 7건 정리, worktree prune
- **Phase 1 (fail-closed)**: `original_identity_binding` 3요소 강제 — 빈 `{}`/부분/SHA 불일치는 `ValueError` (regression: `tests/test_identity_binding_fail_closed.py`)
- **Phase 2 (dedup/sync)**: `verify_profile_sync.ps1` + drift 0 확인
- **Phase 3 (브랜치 통합)**: local merge 완료 (`1dfd791`) — **remote push blocked (WSL credential)**
- **Phase 4 (skill pitfall)**: 검색 엄격성(OR 포함, 0건 조건, fallback 순서) + identity binding fail-closed를 SKILL.md 기록, 4부 sync, 커밋 harness `327e0a0` / profile `3434641`
- **Phase 5 (백업)**: `second-brain-backup-2026-08-17` 생성 + 검증 PASS + README

### 미완료 / blocked
| 항목 | 상태 | 해제 조건 |
|---|---|---|
| Remote push (harness + profile) | blocked | Windows 세션 + Git Credential Manager 인증 |
| **Phase 6 cross-runtime benchmark** | pending-external (0/60) | Codex·Claude 두 runtime으로 동일 query 집합 실행 + artifact 동등성. `research/global-insight-acceptance-audit.md` L47 |
| receipt 5건 repair (profile repo) | **폐기 — gitignored by design** (`data/boi/private/*/`은 0000000 fixture만 tracked) | `e2e-verification-2026-08-17.json` open_items — git 관리 불가. **백업에 포함 확인됨** (해시 일치) |

### Phase 6 spec (재현용)
- 대상: `global-insight-acceptance-audit.md`의 `pending-external` 항목 — "실제 Codex·Claude 동일 입력 반복 evidence (각 새 Case benchmark 0/60)"
- 절차: 새 세션에서 두 runtime으로 **동일 query 집합** 실행 → artifact 동등성 검증 → audit 항목 `pending-external` → `proven`
- 주의: Local Private 콘텐츠 포함 금지 (release tree 제외 규칙)

---

## 5. 신규 세션 체크리스트

1. 이 문서 + `docs/superpowers/plans/2026-08-17-second-brain-improvement-plan.md` 읽기
2. `git -C C:\AI\second-brain status` / `git -C C:\AI\second-brain-2055186 status` 확인
3. Phase 2 스크립트로 sync 재확인 (drift 0 유지 여부)
4. Windows면 `pytest tests/ -q` 전체 green 확인, WSL이면 14건 환경 실패 인지
5. 미완료 표(§4)에서 다음 작업 선택
